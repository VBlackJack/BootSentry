<Application x:Class="BootSentry.UI.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:converters="clr-namespace:BootSentry.UI.Converters"
             StartupUri="Views/MainWindow.xaml"
             ShutdownMode="OnMainWindowClose">
    <Application.Resources>
        <ResourceDictionary>
            <!-- Converters -->
            <converters:BoolToVisibilityConverter x:Key="BoolToVisibility"/>
            <converters:NullToVisibilityConverter x:Key="NullToVisibility"/>
            <converters:NotNullToVisibilityConverter x:Key="NotNullToVisibility"/>
            <converters:InverseBoolConverter x:Key="InverseBool"/>
            <!-- Colors -->
            <Color x:Key="PrimaryColor">#0078D4</Color>
            <Color x:Key="SuccessColor">#107C10</Color>
            <Color x:Key="WarningColor">#FF8C00</Color>
            <Color x:Key="DangerColor">#D13438</Color>
            <Color x:Key="NeutralColor">#6B6B6B</Color>

            <SolidColorBrush x:Key="PrimaryBrush" Color="{StaticResource PrimaryColor}"/>
            <SolidColorBrush x:Key="SuccessBrush" Color="{StaticResource SuccessColor}"/>
            <SolidColorBrush x:Key="WarningBrush" Color="{StaticResource WarningColor}"/>
            <SolidColorBrush x:Key="DangerBrush" Color="{StaticResource DangerColor}"/>
            <SolidColorBrush x:Key="NeutralBrush" Color="{StaticResource NeutralColor}"/>

            <!-- Risk Level Colors -->
            <SolidColorBrush x:Key="RiskSafeBrush" Color="#107C10"/>
            <SolidColorBrush x:Key="RiskUnknownBrush" Color="#6B6B6B"/>
            <SolidColorBrush x:Key="RiskSuspiciousBrush" Color="#FF8C00"/>
            <SolidColorBrush x:Key="RiskCriticalBrush" Color="#D13438"/>

            <!-- Status Colors -->
            <SolidColorBrush x:Key="StatusEnabledBrush" Color="#107C10"/>
            <SolidColorBrush x:Key="StatusDisabledBrush" Color="#6B6B6B"/>
            <SolidColorBrush x:Key="StatusUnknownBrush" Color="#FF8C00"/>

            <!-- Default Button Style -->
            <Style TargetType="Button">
                <Setter Property="Padding" Value="16,8"/>
                <Setter Property="Margin" Value="4"/>
                <Setter Property="Background" Value="{StaticResource PrimaryBrush}"/>
                <Setter Property="Foreground" Value="White"/>
                <Setter Property="BorderThickness" Value="0"/>
                <Setter Property="Cursor" Value="Hand"/>
            </Style>

            <!-- DataGrid Style -->
            <Style TargetType="DataGrid">
                <Setter Property="AutoGenerateColumns" Value="False"/>
                <Setter Property="IsReadOnly" Value="True"/>
                <Setter Property="SelectionMode" Value="Extended"/>
                <Setter Property="CanUserAddRows" Value="False"/>
                <Setter Property="CanUserDeleteRows" Value="False"/>
                <Setter Property="GridLinesVisibility" Value="Horizontal"/>
                <Setter Property="AlternatingRowBackground" Value="#F8F8F8"/>
                <Setter Property="RowHeight" Value="32"/>
                <Setter Property="VirtualizingPanel.IsVirtualizing" Value="True"/>
                <Setter Property="VirtualizingPanel.VirtualizationMode" Value="Recycling"/>
            </Style>
        </ResourceDictionary>
    </Application.Resources>
</Application>
